FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

COPY install.sh /tmp/install.sh
RUN chmod +x /tmp/install.sh

CMD ["bash", "/tmp/install.sh", "--non-interactive", "--no-service", "--tmdb-key", "test_dummy_key_12345"]
